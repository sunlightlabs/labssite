blogdor: subscribe to comments (Jeremy)
openid: reset password (email)
openid: google/yahoo login (Ali)

audit:
    test email
    reduce #queries
    add caching tags
    add titles
    first names instead of usernames
