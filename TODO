blogdor:
    subscribe to comments
projects:
    /projects/add/
    /projects/slug/edit/
    project tag view
users:
    contact user button (email them)
    show current avatar on edit profile page
    fancy skills widget
    implement openid + clickpass
ideas:
    voting
other:
    caching
    test judgeforamerica
    first names instead of usernames
    add redirect from /contests/ to latest contest

Ali:
on edit_profile page, added a (view profile) link
styling on events pages
on tweets, we're showing 'sunlightlabs' at the top of each tweet.. perhaps something else could fit here?
